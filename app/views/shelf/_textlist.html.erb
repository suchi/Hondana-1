<div id="list">
  <ul>
    <% entries.each { |entry| %>
      <li>
        <%= link_to entry.book.title, :isbn => entry.book.isbn , :action => 'edit' %>
        <%= "(#{entry.book.authors} / #{entry.book.publisher})" %>
      </li>
    <% } %>
  </ul>
</div>
