<%#
   本棚の書籍リスト
%>

<%= render 'header', shelf: shelf, entries: entries  %>

<div id='book_volumes'>
  (<%= shelf.countbook_comment %>/<%= shelf.countbook %>)冊
</div>

<div id="shelf_v">
  <div id="shelf">
    <div class="shelf_h"><%= image_tag "/images/fms.gif", :class => "fms" %></div>
    <%= render "#{shelf.listtype}list", entries: entries %> <%#  'textlist', 'imagelist', etc. %>
  </div>
</div>

<div id="shelf_shade"></div>

<%= render 'footer', entries: entries %>
